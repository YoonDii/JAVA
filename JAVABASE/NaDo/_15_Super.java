package JAVABASE.NaDo;

public class _15_Super {
    public static void main(String[] args) {
        //super
        //부모클래스를 먼저 수행하고 자식클래스를 수행한다.
        //super.은 부모클래스의 변수나 메서드에 바로 접근하는 것이고
        //super()은 부모클래스의 생성자에 바로 접근하는 것이다.
    }
}
